// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface Application {
  id: string;
  name: string;
  ownerId: string;
  clientId: string;
  scopes: string[];
  active: boolean;
  createdAt: Date | null;
}

export interface ApplicationCreate {
  name: string;
  ownerId: string;
  clientId: string;
  scopes: string[];
  active: boolean;
}

export interface ApplicationUpdate {
  name?: string;
  ownerId?: string;
  clientId?: string;
  scopes?: string[];
  active?: boolean;
  createdAt?: Date | null;
}

export const ApplicationFields = ['id', 'name', 'ownerId', 'clientId', 'scopes', 'active', 'createdAt'] as const;
export type ApplicationField = (typeof ApplicationFields)[number];

/** Construct a new Application with sensible defaults for optional fields. */
export function makeApplication(input: Partial<Application> & { id: string }): Application {
  return {
    id: input.id,
    name: input.name ?? '',
    ownerId: input.ownerId ?? '',
    clientId: input.clientId ?? '',
    scopes: input.scopes ?? [],
    active: input.active ?? false,
    createdAt: input.createdAt ?? null,
  } as Application;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updateApplication(current: Application, patch: ApplicationUpdate): Application {
  const merged: Application = { ...current };
  for (const key of Object.keys(patch) as ApplicationField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickApplicationFields(entity: Application, fields: ApplicationField[]): Partial<Application> {
  const out: Partial<Application> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function cloneApplication(entity: Application): Application {
  return JSON.parse(JSON.stringify(entity)) as Application;
}
