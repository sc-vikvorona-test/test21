// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Artifact, ArtifactCreate, ArtifactUpdate } from './model';
import { makeArtifact, updateArtifact } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class ArtifactRepository {
  private readonly byId = new Map<string, Artifact>();
  private nextId = 1;

  all(): Artifact[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Artifact | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Artifact {
    const found = this.byId.get(id);
    if (!found) throw new ArtifactNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Artifact) => boolean): Artifact | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Artifact) => boolean): Artifact[] {
    const out: Artifact[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Artifact[] {
    const out: Artifact[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: ArtifactCreate): Artifact {
    const id = `${this.nextId++}`;
    const entity = makeArtifact({ ...create, id } as Partial<Artifact> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: ArtifactUpdate): Artifact {
    const current = this.requireById(id);
    const next = updateArtifact(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: ArtifactCreate, predicate: (existing: Artifact) => boolean): Artifact {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Artifact>(key: K, order: 'asc' | 'desc' = 'asc'): Artifact[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: ArtifactCreate[]): Artifact[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: ArtifactUpdate }>): Artifact[] {
    const out: Artifact[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Artifact[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class ArtifactNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Artifact not found: ${id}`);
    this.name = 'ArtifactNotFoundError';
  }
}
