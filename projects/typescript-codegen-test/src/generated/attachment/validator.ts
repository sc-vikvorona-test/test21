// AUTOGENERATED from codegen/entities.yaml + codegen/templates/validator.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { AttachmentCreate, AttachmentUpdate } from './model';

export function validateAttachmentCreate(input: AttachmentCreate): string[] {
  const errors: string[] = [];
  if (input.parentId !== undefined && typeof input.parentId !== 'string') errors.push('parentId must be a string');
  if (input.parentKind !== undefined && typeof input.parentKind !== 'string') errors.push('parentKind must be a string');
  if (input.name !== undefined && typeof input.name !== 'string') errors.push('name must be a string');
  if (input.contentType !== undefined && typeof input.contentType !== 'string') errors.push('contentType must be a string');
  if (input.sizeCents !== undefined && typeof input.sizeCents !== 'number') errors.push('sizeCents must be a number');
  if (input.uploadedAt !== undefined && input.uploadedAt !== null && !(input.uploadedAt instanceof Date)) errors.push('uploadedAt must be a Date or null');
  return errors;
}

export function validateAttachmentUpdate(input: AttachmentUpdate): string[] {
  const errors: string[] = [];
  if (input.parentId !== undefined && typeof input.parentId !== 'string') errors.push('parentId must be a string');
  if (input.parentKind !== undefined && typeof input.parentKind !== 'string') errors.push('parentKind must be a string');
  if (input.name !== undefined && typeof input.name !== 'string') errors.push('name must be a string');
  if (input.contentType !== undefined && typeof input.contentType !== 'string') errors.push('contentType must be a string');
  if (input.sizeCents !== undefined && typeof input.sizeCents !== 'number') errors.push('sizeCents must be a number');
  if (input.uploadedAt !== undefined && input.uploadedAt !== null && !(input.uploadedAt instanceof Date)) errors.push('uploadedAt must be a Date or null');
  return errors;
}

export function isValidAttachmentCreate(input: AttachmentCreate): boolean {
  return validateAttachmentCreate(input).length === 0;
}

export function isValidAttachmentUpdate(input: AttachmentUpdate): boolean {
  return validateAttachmentUpdate(input).length === 0;
}

export function isKnownAttachmentField(field: string): boolean {
  return [
    'id',
    'parentId',
    'parentKind',
    'name',
    'contentType',
    'sizeCents',
    'uploadedAt',
  ].includes(field);
}
