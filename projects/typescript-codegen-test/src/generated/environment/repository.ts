// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Environment, EnvironmentCreate, EnvironmentUpdate } from './model';
import { makeEnvironment, updateEnvironment } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class EnvironmentRepository {
  private readonly byId = new Map<string, Environment>();
  private nextId = 1;

  all(): Environment[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Environment | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Environment {
    const found = this.byId.get(id);
    if (!found) throw new EnvironmentNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Environment) => boolean): Environment | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Environment) => boolean): Environment[] {
    const out: Environment[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Environment[] {
    const out: Environment[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: EnvironmentCreate): Environment {
    const id = `${this.nextId++}`;
    const entity = makeEnvironment({ ...create, id } as Partial<Environment> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: EnvironmentUpdate): Environment {
    const current = this.requireById(id);
    const next = updateEnvironment(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: EnvironmentCreate, predicate: (existing: Environment) => boolean): Environment {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Environment>(key: K, order: 'asc' | 'desc' = 'asc'): Environment[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: EnvironmentCreate[]): Environment[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: EnvironmentUpdate }>): Environment[] {
    const out: Environment[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Environment[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class EnvironmentNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Environment not found: ${id}`);
    this.name = 'EnvironmentNotFoundError';
  }
}
