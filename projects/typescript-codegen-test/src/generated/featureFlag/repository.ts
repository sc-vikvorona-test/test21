// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { FeatureFlag, FeatureFlagCreate, FeatureFlagUpdate } from './model';
import { makeFeatureFlag, updateFeatureFlag } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class FeatureFlagRepository {
  private readonly byId = new Map<string, FeatureFlag>();
  private nextId = 1;

  all(): FeatureFlag[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): FeatureFlag | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): FeatureFlag {
    const found = this.byId.get(id);
    if (!found) throw new FeatureFlagNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: FeatureFlag) => boolean): FeatureFlag | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: FeatureFlag) => boolean): FeatureFlag[] {
    const out: FeatureFlag[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): FeatureFlag[] {
    const out: FeatureFlag[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: FeatureFlagCreate): FeatureFlag {
    const id = `${this.nextId++}`;
    const entity = makeFeatureFlag({ ...create, id } as Partial<FeatureFlag> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: FeatureFlagUpdate): FeatureFlag {
    const current = this.requireById(id);
    const next = updateFeatureFlag(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: FeatureFlagCreate, predicate: (existing: FeatureFlag) => boolean): FeatureFlag {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof FeatureFlag>(key: K, order: 'asc' | 'desc' = 'asc'): FeatureFlag[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: FeatureFlagCreate[]): FeatureFlag[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: FeatureFlagUpdate }>): FeatureFlag[] {
    const out: FeatureFlag[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: FeatureFlag[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class FeatureFlagNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`FeatureFlag not found: ${id}`);
    this.name = 'FeatureFlagNotFoundError';
  }
}
