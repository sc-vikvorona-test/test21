// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Integration, IntegrationCreate, IntegrationUpdate } from './model';
import { makeIntegration, updateIntegration } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class IntegrationRepository {
  private readonly byId = new Map<string, Integration>();
  private nextId = 1;

  all(): Integration[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Integration | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Integration {
    const found = this.byId.get(id);
    if (!found) throw new IntegrationNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Integration) => boolean): Integration | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Integration) => boolean): Integration[] {
    const out: Integration[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Integration[] {
    const out: Integration[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: IntegrationCreate): Integration {
    const id = `${this.nextId++}`;
    const entity = makeIntegration({ ...create, id } as Partial<Integration> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: IntegrationUpdate): Integration {
    const current = this.requireById(id);
    const next = updateIntegration(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: IntegrationCreate, predicate: (existing: Integration) => boolean): Integration {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Integration>(key: K, order: 'asc' | 'desc' = 'asc'): Integration[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: IntegrationCreate[]): Integration[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: IntegrationUpdate }>): Integration[] {
    const out: Integration[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Integration[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class IntegrationNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Integration not found: ${id}`);
    this.name = 'IntegrationNotFoundError';
  }
}
