// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface Invitation {
  id: string;
  email: string;
  organizationId: string;
  inviterId: string;
  token: string;
  acceptedAt: Date | null;
  expiresAt: Date | null;
}

export interface InvitationCreate {
  email: string;
  organizationId: string;
  inviterId: string;
  token: string;
  acceptedAt: Date | null;
  expiresAt: Date | null;
}

export interface InvitationUpdate {
  email?: string;
  organizationId?: string;
  inviterId?: string;
  token?: string;
  acceptedAt?: Date | null;
  expiresAt?: Date | null;
}

export const InvitationFields = ['id', 'email', 'organizationId', 'inviterId', 'token', 'acceptedAt', 'expiresAt'] as const;
export type InvitationField = (typeof InvitationFields)[number];

/** Construct a new Invitation with sensible defaults for optional fields. */
export function makeInvitation(input: Partial<Invitation> & { id: string }): Invitation {
  return {
    id: input.id,
    email: input.email ?? '',
    organizationId: input.organizationId ?? '',
    inviterId: input.inviterId ?? '',
    token: input.token ?? '',
    acceptedAt: input.acceptedAt ?? null,
    expiresAt: input.expiresAt ?? null,
  } as Invitation;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updateInvitation(current: Invitation, patch: InvitationUpdate): Invitation {
  const merged: Invitation = { ...current };
  for (const key of Object.keys(patch) as InvitationField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickInvitationFields(entity: Invitation, fields: InvitationField[]): Partial<Invitation> {
  const out: Partial<Invitation> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function cloneInvitation(entity: Invitation): Invitation {
  return JSON.parse(JSON.stringify(entity)) as Invitation;
}
