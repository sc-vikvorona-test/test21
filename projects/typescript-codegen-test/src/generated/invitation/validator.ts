// AUTOGENERATED from codegen/entities.yaml + codegen/templates/validator.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { InvitationCreate, InvitationUpdate } from './model';

export function validateInvitationCreate(input: InvitationCreate): string[] {
  const errors: string[] = [];
  if (input.email !== undefined && typeof input.email !== 'string') errors.push('email must be a string');
  if (input.organizationId !== undefined && typeof input.organizationId !== 'string') errors.push('organizationId must be a string');
  if (input.inviterId !== undefined && typeof input.inviterId !== 'string') errors.push('inviterId must be a string');
  if (input.token !== undefined && typeof input.token !== 'string') errors.push('token must be a string');
  if (input.acceptedAt !== undefined && input.acceptedAt !== null && !(input.acceptedAt instanceof Date)) errors.push('acceptedAt must be a Date or null');
  if (input.expiresAt !== undefined && input.expiresAt !== null && !(input.expiresAt instanceof Date)) errors.push('expiresAt must be a Date or null');
  return errors;
}

export function validateInvitationUpdate(input: InvitationUpdate): string[] {
  const errors: string[] = [];
  if (input.email !== undefined && typeof input.email !== 'string') errors.push('email must be a string');
  if (input.organizationId !== undefined && typeof input.organizationId !== 'string') errors.push('organizationId must be a string');
  if (input.inviterId !== undefined && typeof input.inviterId !== 'string') errors.push('inviterId must be a string');
  if (input.token !== undefined && typeof input.token !== 'string') errors.push('token must be a string');
  if (input.acceptedAt !== undefined && input.acceptedAt !== null && !(input.acceptedAt instanceof Date)) errors.push('acceptedAt must be a Date or null');
  if (input.expiresAt !== undefined && input.expiresAt !== null && !(input.expiresAt instanceof Date)) errors.push('expiresAt must be a Date or null');
  return errors;
}

export function isValidInvitationCreate(input: InvitationCreate): boolean {
  return validateInvitationCreate(input).length === 0;
}

export function isValidInvitationUpdate(input: InvitationUpdate): boolean {
  return validateInvitationUpdate(input).length === 0;
}

export function isKnownInvitationField(field: string): boolean {
  return [
    'id',
    'email',
    'organizationId',
    'inviterId',
    'token',
    'acceptedAt',
    'expiresAt',
  ].includes(field);
}
