// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface Invoice {
  id: string;
  billingAccountId: string;
  amountCents: number;
  currency: string;
  status: string;
  paidAt: Date | null;
  dueAt: Date | null;
}

export interface InvoiceCreate {
  billingAccountId: string;
  amountCents: number;
  currency: string;
  status: string;
  paidAt: Date | null;
  dueAt: Date | null;
}

export interface InvoiceUpdate {
  billingAccountId?: string;
  amountCents?: number;
  currency?: string;
  status?: string;
  paidAt?: Date | null;
  dueAt?: Date | null;
}

export const InvoiceFields = ['id', 'billingAccountId', 'amountCents', 'currency', 'status', 'paidAt', 'dueAt'] as const;
export type InvoiceField = (typeof InvoiceFields)[number];

/** Construct a new Invoice with sensible defaults for optional fields. */
export function makeInvoice(input: Partial<Invoice> & { id: string }): Invoice {
  return {
    id: input.id,
    billingAccountId: input.billingAccountId ?? '',
    amountCents: input.amountCents ?? 0,
    currency: input.currency ?? '',
    status: input.status ?? '',
    paidAt: input.paidAt ?? null,
    dueAt: input.dueAt ?? null,
  } as Invoice;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updateInvoice(current: Invoice, patch: InvoiceUpdate): Invoice {
  const merged: Invoice = { ...current };
  for (const key of Object.keys(patch) as InvoiceField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickInvoiceFields(entity: Invoice, fields: InvoiceField[]): Partial<Invoice> {
  const out: Partial<Invoice> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function cloneInvoice(entity: Invoice): Invoice {
  return JSON.parse(JSON.stringify(entity)) as Invoice;
}
