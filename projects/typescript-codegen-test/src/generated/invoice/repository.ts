// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Invoice, InvoiceCreate, InvoiceUpdate } from './model';
import { makeInvoice, updateInvoice } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class InvoiceRepository {
  private readonly byId = new Map<string, Invoice>();
  private nextId = 1;

  all(): Invoice[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Invoice | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Invoice {
    const found = this.byId.get(id);
    if (!found) throw new InvoiceNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Invoice) => boolean): Invoice | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Invoice) => boolean): Invoice[] {
    const out: Invoice[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Invoice[] {
    const out: Invoice[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: InvoiceCreate): Invoice {
    const id = `${this.nextId++}`;
    const entity = makeInvoice({ ...create, id } as Partial<Invoice> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: InvoiceUpdate): Invoice {
    const current = this.requireById(id);
    const next = updateInvoice(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: InvoiceCreate, predicate: (existing: Invoice) => boolean): Invoice {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Invoice>(key: K, order: 'asc' | 'desc' = 'asc'): Invoice[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: InvoiceCreate[]): Invoice[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: InvoiceUpdate }>): Invoice[] {
    const out: Invoice[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Invoice[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class InvoiceNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Invoice not found: ${id}`);
    this.name = 'InvoiceNotFoundError';
  }
}
