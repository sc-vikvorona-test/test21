// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Membership, MembershipCreate, MembershipUpdate } from './model';
import { makeMembership, updateMembership } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class MembershipRepository {
  private readonly byId = new Map<string, Membership>();
  private nextId = 1;

  all(): Membership[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Membership | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Membership {
    const found = this.byId.get(id);
    if (!found) throw new MembershipNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Membership) => boolean): Membership | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Membership) => boolean): Membership[] {
    const out: Membership[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Membership[] {
    const out: Membership[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: MembershipCreate): Membership {
    const id = `${this.nextId++}`;
    const entity = makeMembership({ ...create, id } as Partial<Membership> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: MembershipUpdate): Membership {
    const current = this.requireById(id);
    const next = updateMembership(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: MembershipCreate, predicate: (existing: Membership) => boolean): Membership {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Membership>(key: K, order: 'asc' | 'desc' = 'asc'): Membership[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: MembershipCreate[]): Membership[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: MembershipUpdate }>): Membership[] {
    const out: Membership[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Membership[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class MembershipNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Membership not found: ${id}`);
    this.name = 'MembershipNotFoundError';
  }
}
