// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface Mention {
  id: string;
  subjectId: string;
  subjectKind: string;
  userId: string;
  acknowledgedAt: Date | null;
  createdAt: Date | null;
}

export interface MentionCreate {
  subjectId: string;
  subjectKind: string;
  userId: string;
  acknowledgedAt: Date | null;
}

export interface MentionUpdate {
  subjectId?: string;
  subjectKind?: string;
  userId?: string;
  acknowledgedAt?: Date | null;
  createdAt?: Date | null;
}

export const MentionFields = ['id', 'subjectId', 'subjectKind', 'userId', 'acknowledgedAt', 'createdAt'] as const;
export type MentionField = (typeof MentionFields)[number];

/** Construct a new Mention with sensible defaults for optional fields. */
export function makeMention(input: Partial<Mention> & { id: string }): Mention {
  return {
    id: input.id,
    subjectId: input.subjectId ?? '',
    subjectKind: input.subjectKind ?? '',
    userId: input.userId ?? '',
    acknowledgedAt: input.acknowledgedAt ?? null,
    createdAt: input.createdAt ?? null,
  } as Mention;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updateMention(current: Mention, patch: MentionUpdate): Mention {
  const merged: Mention = { ...current };
  for (const key of Object.keys(patch) as MentionField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickMentionFields(entity: Mention, fields: MentionField[]): Partial<Mention> {
  const out: Partial<Mention> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function cloneMention(entity: Mention): Mention {
  return JSON.parse(JSON.stringify(entity)) as Mention;
}
