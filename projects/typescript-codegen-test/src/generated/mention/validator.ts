// AUTOGENERATED from codegen/entities.yaml + codegen/templates/validator.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { MentionCreate, MentionUpdate } from './model';

export function validateMentionCreate(input: MentionCreate): string[] {
  const errors: string[] = [];
  if (input.subjectId !== undefined && typeof input.subjectId !== 'string') errors.push('subjectId must be a string');
  if (input.subjectKind !== undefined && typeof input.subjectKind !== 'string') errors.push('subjectKind must be a string');
  if (input.userId !== undefined && typeof input.userId !== 'string') errors.push('userId must be a string');
  if (input.acknowledgedAt !== undefined && input.acknowledgedAt !== null && !(input.acknowledgedAt instanceof Date)) errors.push('acknowledgedAt must be a Date or null');
  if (input.createdAt !== undefined && input.createdAt !== null && !(input.createdAt instanceof Date)) errors.push('createdAt must be a Date or null');
  return errors;
}

export function validateMentionUpdate(input: MentionUpdate): string[] {
  const errors: string[] = [];
  if (input.subjectId !== undefined && typeof input.subjectId !== 'string') errors.push('subjectId must be a string');
  if (input.subjectKind !== undefined && typeof input.subjectKind !== 'string') errors.push('subjectKind must be a string');
  if (input.userId !== undefined && typeof input.userId !== 'string') errors.push('userId must be a string');
  if (input.acknowledgedAt !== undefined && input.acknowledgedAt !== null && !(input.acknowledgedAt instanceof Date)) errors.push('acknowledgedAt must be a Date or null');
  if (input.createdAt !== undefined && input.createdAt !== null && !(input.createdAt instanceof Date)) errors.push('createdAt must be a Date or null');
  return errors;
}

export function isValidMentionCreate(input: MentionCreate): boolean {
  return validateMentionCreate(input).length === 0;
}

export function isValidMentionUpdate(input: MentionUpdate): boolean {
  return validateMentionUpdate(input).length === 0;
}

export function isKnownMentionField(field: string): boolean {
  return [
    'id',
    'subjectId',
    'subjectKind',
    'userId',
    'acknowledgedAt',
    'createdAt',
  ].includes(field);
}
