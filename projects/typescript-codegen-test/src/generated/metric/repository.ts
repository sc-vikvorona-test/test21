// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Metric, MetricCreate, MetricUpdate } from './model';
import { makeMetric, updateMetric } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class MetricRepository {
  private readonly byId = new Map<string, Metric>();
  private nextId = 1;

  all(): Metric[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Metric | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Metric {
    const found = this.byId.get(id);
    if (!found) throw new MetricNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Metric) => boolean): Metric | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Metric) => boolean): Metric[] {
    const out: Metric[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Metric[] {
    const out: Metric[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: MetricCreate): Metric {
    const id = `${this.nextId++}`;
    const entity = makeMetric({ ...create, id } as Partial<Metric> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: MetricUpdate): Metric {
    const current = this.requireById(id);
    const next = updateMetric(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: MetricCreate, predicate: (existing: Metric) => boolean): Metric {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Metric>(key: K, order: 'asc' | 'desc' = 'asc'): Metric[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: MetricCreate[]): Metric[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: MetricUpdate }>): Metric[] {
    const out: Metric[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Metric[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class MetricNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Metric not found: ${id}`);
    this.name = 'MetricNotFoundError';
  }
}
