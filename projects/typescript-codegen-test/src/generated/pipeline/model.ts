// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface Pipeline {
  id: string;
  name: string;
  projectId: string;
  definition: string;
  active: boolean;
  createdAt: Date | null;
}

export interface PipelineCreate {
  name: string;
  projectId: string;
  definition: string;
  active: boolean;
}

export interface PipelineUpdate {
  name?: string;
  projectId?: string;
  definition?: string;
  active?: boolean;
  createdAt?: Date | null;
}

export const PipelineFields = ['id', 'name', 'projectId', 'definition', 'active', 'createdAt'] as const;
export type PipelineField = (typeof PipelineFields)[number];

/** Construct a new Pipeline with sensible defaults for optional fields. */
export function makePipeline(input: Partial<Pipeline> & { id: string }): Pipeline {
  return {
    id: input.id,
    name: input.name ?? '',
    projectId: input.projectId ?? '',
    definition: input.definition ?? '',
    active: input.active ?? false,
    createdAt: input.createdAt ?? null,
  } as Pipeline;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updatePipeline(current: Pipeline, patch: PipelineUpdate): Pipeline {
  const merged: Pipeline = { ...current };
  for (const key of Object.keys(patch) as PipelineField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickPipelineFields(entity: Pipeline, fields: PipelineField[]): Partial<Pipeline> {
  const out: Partial<Pipeline> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function clonePipeline(entity: Pipeline): Pipeline {
  return JSON.parse(JSON.stringify(entity)) as Pipeline;
}
