// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface PipelineRun {
  id: string;
  pipelineId: string;
  status: string;
  triggeredBy: string;
  startedAt: Date | null;
  completedAt: Date | null;
}

export interface PipelineRunCreate {
  pipelineId: string;
  status: string;
  triggeredBy: string;
  startedAt: Date | null;
  completedAt: Date | null;
}

export interface PipelineRunUpdate {
  pipelineId?: string;
  status?: string;
  triggeredBy?: string;
  startedAt?: Date | null;
  completedAt?: Date | null;
}

export const PipelineRunFields = ['id', 'pipelineId', 'status', 'triggeredBy', 'startedAt', 'completedAt'] as const;
export type PipelineRunField = (typeof PipelineRunFields)[number];

/** Construct a new PipelineRun with sensible defaults for optional fields. */
export function makePipelineRun(input: Partial<PipelineRun> & { id: string }): PipelineRun {
  return {
    id: input.id,
    pipelineId: input.pipelineId ?? '',
    status: input.status ?? '',
    triggeredBy: input.triggeredBy ?? '',
    startedAt: input.startedAt ?? null,
    completedAt: input.completedAt ?? null,
  } as PipelineRun;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updatePipelineRun(current: PipelineRun, patch: PipelineRunUpdate): PipelineRun {
  const merged: PipelineRun = { ...current };
  for (const key of Object.keys(patch) as PipelineRunField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickPipelineRunFields(entity: PipelineRun, fields: PipelineRunField[]): Partial<PipelineRun> {
  const out: Partial<PipelineRun> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function clonePipelineRun(entity: PipelineRun): PipelineRun {
  return JSON.parse(JSON.stringify(entity)) as PipelineRun;
}
