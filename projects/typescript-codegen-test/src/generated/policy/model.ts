// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface Policy {
  id: string;
  name: string;
  ownerId: string;
  definition: string;
  active: boolean;
  createdAt: Date | null;
}

export interface PolicyCreate {
  name: string;
  ownerId: string;
  definition: string;
  active: boolean;
}

export interface PolicyUpdate {
  name?: string;
  ownerId?: string;
  definition?: string;
  active?: boolean;
  createdAt?: Date | null;
}

export const PolicyFields = ['id', 'name', 'ownerId', 'definition', 'active', 'createdAt'] as const;
export type PolicyField = (typeof PolicyFields)[number];

/** Construct a new Policy with sensible defaults for optional fields. */
export function makePolicy(input: Partial<Policy> & { id: string }): Policy {
  return {
    id: input.id,
    name: input.name ?? '',
    ownerId: input.ownerId ?? '',
    definition: input.definition ?? '',
    active: input.active ?? false,
    createdAt: input.createdAt ?? null,
  } as Policy;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updatePolicy(current: Policy, patch: PolicyUpdate): Policy {
  const merged: Policy = { ...current };
  for (const key of Object.keys(patch) as PolicyField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickPolicyFields(entity: Policy, fields: PolicyField[]): Partial<Policy> {
  const out: Partial<Policy> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function clonePolicy(entity: Policy): Policy {
  return JSON.parse(JSON.stringify(entity)) as Policy;
}
