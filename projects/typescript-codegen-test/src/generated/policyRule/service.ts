// AUTOGENERATED from codegen/entities.yaml + codegen/templates/service.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { PolicyRule, PolicyRuleCreate, PolicyRuleUpdate } from './model';
import { PolicyRuleRepository, PolicyRuleNotFoundError } from './repository';
import { validatePolicyRuleCreate, validatePolicyRuleUpdate } from './validator';

export interface PolicyRuleServiceDeps {
  repository: PolicyRuleRepository;
  clock?: () => Date;
  logger?: (msg: string, meta?: Record<string, unknown>) => void;
}

export interface ListPolicyRuleOptions {
  offset?: number;
  limit?: number;
  sortBy?: keyof PolicyRule;
  order?: 'asc' | 'desc';
}

/**
 * Business-logic layer above PolicyRuleRepository.
 */
export class PolicyRuleService {
  private readonly repo: PolicyRuleRepository;
  private readonly clock: () => Date;
  private readonly logger: (msg: string, meta?: Record<string, unknown>) => void;

  constructor(deps: PolicyRuleServiceDeps) {
    this.repo = deps.repository;
    this.clock = deps.clock ?? (() => new Date());
    this.logger = deps.logger ?? (() => { /* noop */ });
  }

  create(input: PolicyRuleCreate): PolicyRule {
    const errors = validatePolicyRuleCreate(input);
    if (errors.length > 0) throw new PolicyRuleValidationError(errors);
    const created = this.repo.insert(input);
    this.logger('PolicyRuleService.create', { id: created.id });
    return created;
  }

  get(id: string): PolicyRule {
    return this.repo.requireById(id);
  }

  tryGet(id: string): PolicyRule | undefined {
    return this.repo.findById(id);
  }

  update(id: string, patch: PolicyRuleUpdate): PolicyRule {
    const errors = validatePolicyRuleUpdate(patch);
    if (errors.length > 0) throw new PolicyRuleValidationError(errors);
    const updated = this.repo.update(id, patch);
    this.logger('PolicyRuleService.update', { id });
    return updated;
  }

  delete(id: string): void {
    const existed = this.repo.delete(id);
    if (!existed) throw new PolicyRuleNotFoundError(id);
    this.logger('PolicyRuleService.delete', { id });
  }

  list(options: ListPolicyRuleOptions = {}): PolicyRule[] {
    const offset = options.offset ?? 0;
    const limit = options.limit ?? 50;
    if (options.sortBy) {
      const sorted = this.repo.sortedBy(options.sortBy, options.order);
      return sorted.slice(offset, offset + limit);
    }
    return this.repo.paginate(offset, limit);
  }

  countAll(): number {
    return this.repo.count();
  }

  batchCreate(inputs: PolicyRuleCreate[]): PolicyRule[] {
    for (const input of inputs) {
      const errors = validatePolicyRuleCreate(input);
      if (errors.length > 0) throw new PolicyRuleValidationError(errors);
    }
    return this.repo.batchInsert(inputs);
  }

  exists(id: string): boolean {
    return this.repo.findById(id) !== undefined;
  }

  bulkDelete(ids: string[]): number {
    let deleted = 0;
    for (const id of ids) if (this.repo.delete(id)) deleted++;
    return deleted;
  }

  searchByField<K extends keyof PolicyRule>(field: K, value: PolicyRule[K]): PolicyRule[] {
    return this.repo.filter((entity) => entity[field] === value);
  }
}

export class PolicyRuleServiceError extends Error {
  constructor(message: string) {
    super(message);
    this.name = 'PolicyRuleServiceError';
  }
}

export class PolicyRuleValidationError extends Error {
  constructor(public readonly errors: string[]) {
    super(`PolicyRule validation failed: ${errors.join('; ')}`);
    this.name = 'PolicyRuleValidationError';
  }
}
