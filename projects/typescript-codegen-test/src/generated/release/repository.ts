// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Release, ReleaseCreate, ReleaseUpdate } from './model';
import { makeRelease, updateRelease } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class ReleaseRepository {
  private readonly byId = new Map<string, Release>();
  private nextId = 1;

  all(): Release[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Release | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Release {
    const found = this.byId.get(id);
    if (!found) throw new ReleaseNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Release) => boolean): Release | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Release) => boolean): Release[] {
    const out: Release[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Release[] {
    const out: Release[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: ReleaseCreate): Release {
    const id = `${this.nextId++}`;
    const entity = makeRelease({ ...create, id } as Partial<Release> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: ReleaseUpdate): Release {
    const current = this.requireById(id);
    const next = updateRelease(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: ReleaseCreate, predicate: (existing: Release) => boolean): Release {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Release>(key: K, order: 'asc' | 'desc' = 'asc'): Release[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: ReleaseCreate[]): Release[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: ReleaseUpdate }>): Release[] {
    const out: Release[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Release[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class ReleaseNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Release not found: ${id}`);
    this.name = 'ReleaseNotFoundError';
  }
}
