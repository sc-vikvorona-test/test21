// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Repository, RepositoryCreate, RepositoryUpdate } from './model';
import { makeRepository, updateRepository } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class RepositoryRepository {
  private readonly byId = new Map<string, Repository>();
  private nextId = 1;

  all(): Repository[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Repository | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Repository {
    const found = this.byId.get(id);
    if (!found) throw new RepositoryNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Repository) => boolean): Repository | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Repository) => boolean): Repository[] {
    const out: Repository[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Repository[] {
    const out: Repository[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: RepositoryCreate): Repository {
    const id = `${this.nextId++}`;
    const entity = makeRepository({ ...create, id } as Partial<Repository> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: RepositoryUpdate): Repository {
    const current = this.requireById(id);
    const next = updateRepository(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: RepositoryCreate, predicate: (existing: Repository) => boolean): Repository {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Repository>(key: K, order: 'asc' | 'desc' = 'asc'): Repository[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: RepositoryCreate[]): Repository[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: RepositoryUpdate }>): Repository[] {
    const out: Repository[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Repository[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class RepositoryNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Repository not found: ${id}`);
    this.name = 'RepositoryNotFoundError';
  }
}
