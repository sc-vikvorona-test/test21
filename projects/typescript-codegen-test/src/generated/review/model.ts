// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface Review {
  id: string;
  pullRequestId: string;
  reviewerId: string;
  state: string;
  submittedAt: Date | null;
  body: string;
}

export interface ReviewCreate {
  pullRequestId: string;
  reviewerId: string;
  state: string;
  submittedAt: Date | null;
  body: string;
}

export interface ReviewUpdate {
  pullRequestId?: string;
  reviewerId?: string;
  state?: string;
  submittedAt?: Date | null;
  body?: string;
}

export const ReviewFields = ['id', 'pullRequestId', 'reviewerId', 'state', 'submittedAt', 'body'] as const;
export type ReviewField = (typeof ReviewFields)[number];

/** Construct a new Review with sensible defaults for optional fields. */
export function makeReview(input: Partial<Review> & { id: string }): Review {
  return {
    id: input.id,
    pullRequestId: input.pullRequestId ?? '',
    reviewerId: input.reviewerId ?? '',
    state: input.state ?? '',
    submittedAt: input.submittedAt ?? null,
    body: input.body ?? '',
  } as Review;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updateReview(current: Review, patch: ReviewUpdate): Review {
  const merged: Review = { ...current };
  for (const key of Object.keys(patch) as ReviewField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickReviewFields(entity: Review, fields: ReviewField[]): Partial<Review> {
  const out: Partial<Review> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function cloneReview(entity: Review): Review {
  return JSON.parse(JSON.stringify(entity)) as Review;
}
