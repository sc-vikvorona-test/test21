// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Review, ReviewCreate, ReviewUpdate } from './model';
import { makeReview, updateReview } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class ReviewRepository {
  private readonly byId = new Map<string, Review>();
  private nextId = 1;

  all(): Review[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Review | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Review {
    const found = this.byId.get(id);
    if (!found) throw new ReviewNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Review) => boolean): Review | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Review) => boolean): Review[] {
    const out: Review[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Review[] {
    const out: Review[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: ReviewCreate): Review {
    const id = `${this.nextId++}`;
    const entity = makeReview({ ...create, id } as Partial<Review> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: ReviewUpdate): Review {
    const current = this.requireById(id);
    const next = updateReview(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: ReviewCreate, predicate: (existing: Review) => boolean): Review {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Review>(key: K, order: 'asc' | 'desc' = 'asc'): Review[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: ReviewCreate[]): Review[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: ReviewUpdate }>): Review[] {
    const out: Review[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Review[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class ReviewNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Review not found: ${id}`);
    this.name = 'ReviewNotFoundError';
  }
}
