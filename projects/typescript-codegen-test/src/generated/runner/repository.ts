// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Runner, RunnerCreate, RunnerUpdate } from './model';
import { makeRunner, updateRunner } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class RunnerRepository {
  private readonly byId = new Map<string, Runner>();
  private nextId = 1;

  all(): Runner[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Runner | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Runner {
    const found = this.byId.get(id);
    if (!found) throw new RunnerNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Runner) => boolean): Runner | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Runner) => boolean): Runner[] {
    const out: Runner[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Runner[] {
    const out: Runner[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: RunnerCreate): Runner {
    const id = `${this.nextId++}`;
    const entity = makeRunner({ ...create, id } as Partial<Runner> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: RunnerUpdate): Runner {
    const current = this.requireById(id);
    const next = updateRunner(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: RunnerCreate, predicate: (existing: Runner) => boolean): Runner {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Runner>(key: K, order: 'asc' | 'desc' = 'asc'): Runner[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: RunnerCreate[]): Runner[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: RunnerUpdate }>): Runner[] {
    const out: Runner[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Runner[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class RunnerNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Runner not found: ${id}`);
    this.name = 'RunnerNotFoundError';
  }
}
