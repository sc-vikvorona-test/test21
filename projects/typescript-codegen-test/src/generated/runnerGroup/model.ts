// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface RunnerGroup {
  id: string;
  name: string;
  ownerId: string;
  scopes: string[];
  createdAt: Date | null;
}

export interface RunnerGroupCreate {
  name: string;
  ownerId: string;
  scopes: string[];
}

export interface RunnerGroupUpdate {
  name?: string;
  ownerId?: string;
  scopes?: string[];
  createdAt?: Date | null;
}

export const RunnerGroupFields = ['id', 'name', 'ownerId', 'scopes', 'createdAt'] as const;
export type RunnerGroupField = (typeof RunnerGroupFields)[number];

/** Construct a new RunnerGroup with sensible defaults for optional fields. */
export function makeRunnerGroup(input: Partial<RunnerGroup> & { id: string }): RunnerGroup {
  return {
    id: input.id,
    name: input.name ?? '',
    ownerId: input.ownerId ?? '',
    scopes: input.scopes ?? [],
    createdAt: input.createdAt ?? null,
  } as RunnerGroup;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updateRunnerGroup(current: RunnerGroup, patch: RunnerGroupUpdate): RunnerGroup {
  const merged: RunnerGroup = { ...current };
  for (const key of Object.keys(patch) as RunnerGroupField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickRunnerGroupFields(entity: RunnerGroup, fields: RunnerGroupField[]): Partial<RunnerGroup> {
  const out: Partial<RunnerGroup> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function cloneRunnerGroup(entity: RunnerGroup): RunnerGroup {
  return JSON.parse(JSON.stringify(entity)) as RunnerGroup;
}
