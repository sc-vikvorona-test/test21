// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { RunnerGroup, RunnerGroupCreate, RunnerGroupUpdate } from './model';
import { makeRunnerGroup, updateRunnerGroup } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class RunnerGroupRepository {
  private readonly byId = new Map<string, RunnerGroup>();
  private nextId = 1;

  all(): RunnerGroup[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): RunnerGroup | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): RunnerGroup {
    const found = this.byId.get(id);
    if (!found) throw new RunnerGroupNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: RunnerGroup) => boolean): RunnerGroup | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: RunnerGroup) => boolean): RunnerGroup[] {
    const out: RunnerGroup[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): RunnerGroup[] {
    const out: RunnerGroup[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: RunnerGroupCreate): RunnerGroup {
    const id = `${this.nextId++}`;
    const entity = makeRunnerGroup({ ...create, id } as Partial<RunnerGroup> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: RunnerGroupUpdate): RunnerGroup {
    const current = this.requireById(id);
    const next = updateRunnerGroup(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: RunnerGroupCreate, predicate: (existing: RunnerGroup) => boolean): RunnerGroup {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof RunnerGroup>(key: K, order: 'asc' | 'desc' = 'asc'): RunnerGroup[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: RunnerGroupCreate[]): RunnerGroup[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: RunnerGroupUpdate }>): RunnerGroup[] {
    const out: RunnerGroup[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: RunnerGroup[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class RunnerGroupNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`RunnerGroup not found: ${id}`);
    this.name = 'RunnerGroupNotFoundError';
  }
}
