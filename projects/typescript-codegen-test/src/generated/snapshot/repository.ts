// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Snapshot, SnapshotCreate, SnapshotUpdate } from './model';
import { makeSnapshot, updateSnapshot } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class SnapshotRepository {
  private readonly byId = new Map<string, Snapshot>();
  private nextId = 1;

  all(): Snapshot[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Snapshot | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Snapshot {
    const found = this.byId.get(id);
    if (!found) throw new SnapshotNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Snapshot) => boolean): Snapshot | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Snapshot) => boolean): Snapshot[] {
    const out: Snapshot[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Snapshot[] {
    const out: Snapshot[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: SnapshotCreate): Snapshot {
    const id = `${this.nextId++}`;
    const entity = makeSnapshot({ ...create, id } as Partial<Snapshot> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: SnapshotUpdate): Snapshot {
    const current = this.requireById(id);
    const next = updateSnapshot(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: SnapshotCreate, predicate: (existing: Snapshot) => boolean): Snapshot {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Snapshot>(key: K, order: 'asc' | 'desc' = 'asc'): Snapshot[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: SnapshotCreate[]): Snapshot[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: SnapshotUpdate }>): Snapshot[] {
    const out: Snapshot[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Snapshot[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class SnapshotNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Snapshot not found: ${id}`);
    this.name = 'SnapshotNotFoundError';
  }
}
