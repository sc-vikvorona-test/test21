// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { SshKey, SshKeyCreate, SshKeyUpdate } from './model';
import { makeSshKey, updateSshKey } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class SshKeyRepository {
  private readonly byId = new Map<string, SshKey>();
  private nextId = 1;

  all(): SshKey[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): SshKey | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): SshKey {
    const found = this.byId.get(id);
    if (!found) throw new SshKeyNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: SshKey) => boolean): SshKey | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: SshKey) => boolean): SshKey[] {
    const out: SshKey[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): SshKey[] {
    const out: SshKey[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: SshKeyCreate): SshKey {
    const id = `${this.nextId++}`;
    const entity = makeSshKey({ ...create, id } as Partial<SshKey> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: SshKeyUpdate): SshKey {
    const current = this.requireById(id);
    const next = updateSshKey(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: SshKeyCreate, predicate: (existing: SshKey) => boolean): SshKey {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof SshKey>(key: K, order: 'asc' | 'desc' = 'asc'): SshKey[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: SshKeyCreate[]): SshKey[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: SshKeyUpdate }>): SshKey[] {
    const out: SshKey[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: SshKey[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class SshKeyNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`SshKey not found: ${id}`);
    this.name = 'SshKeyNotFoundError';
  }
}
