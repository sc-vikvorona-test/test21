// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Subscription, SubscriptionCreate, SubscriptionUpdate } from './model';
import { makeSubscription, updateSubscription } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class SubscriptionRepository {
  private readonly byId = new Map<string, Subscription>();
  private nextId = 1;

  all(): Subscription[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Subscription | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Subscription {
    const found = this.byId.get(id);
    if (!found) throw new SubscriptionNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Subscription) => boolean): Subscription | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Subscription) => boolean): Subscription[] {
    const out: Subscription[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Subscription[] {
    const out: Subscription[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: SubscriptionCreate): Subscription {
    const id = `${this.nextId++}`;
    const entity = makeSubscription({ ...create, id } as Partial<Subscription> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: SubscriptionUpdate): Subscription {
    const current = this.requireById(id);
    const next = updateSubscription(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: SubscriptionCreate, predicate: (existing: Subscription) => boolean): Subscription {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Subscription>(key: K, order: 'asc' | 'desc' = 'asc'): Subscription[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: SubscriptionCreate[]): Subscription[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: SubscriptionUpdate }>): Subscription[] {
    const out: Subscription[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Subscription[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class SubscriptionNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Subscription not found: ${id}`);
    this.name = 'SubscriptionNotFoundError';
  }
}
