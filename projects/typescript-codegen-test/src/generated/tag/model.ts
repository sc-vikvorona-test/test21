// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface Tag {
  id: string;
  name: string;
  repositoryId: string;
  commitSha: string;
  message: string;
  createdAt: Date | null;
}

export interface TagCreate {
  name: string;
  repositoryId: string;
  commitSha: string;
  message: string;
}

export interface TagUpdate {
  name?: string;
  repositoryId?: string;
  commitSha?: string;
  message?: string;
  createdAt?: Date | null;
}

export const TagFields = ['id', 'name', 'repositoryId', 'commitSha', 'message', 'createdAt'] as const;
export type TagField = (typeof TagFields)[number];

/** Construct a new Tag with sensible defaults for optional fields. */
export function makeTag(input: Partial<Tag> & { id: string }): Tag {
  return {
    id: input.id,
    name: input.name ?? '',
    repositoryId: input.repositoryId ?? '',
    commitSha: input.commitSha ?? '',
    message: input.message ?? '',
    createdAt: input.createdAt ?? null,
  } as Tag;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updateTag(current: Tag, patch: TagUpdate): Tag {
  const merged: Tag = { ...current };
  for (const key of Object.keys(patch) as TagField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickTagFields(entity: Tag, fields: TagField[]): Partial<Tag> {
  const out: Partial<Tag> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function cloneTag(entity: Tag): Tag {
  return JSON.parse(JSON.stringify(entity)) as Tag;
}
