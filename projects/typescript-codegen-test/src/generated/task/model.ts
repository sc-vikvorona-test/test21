// AUTOGENERATED from codegen/entities.yaml + codegen/templates/model.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

export interface Task {
  id: string;
  title: string;
  projectId: string;
  assigneeId: string;
  status: string;
  priority: number;
  dueAt: Date | null;
  createdAt: Date | null;
}

export interface TaskCreate {
  title: string;
  projectId: string;
  assigneeId: string;
  status: string;
  priority: number;
  dueAt: Date | null;
}

export interface TaskUpdate {
  title?: string;
  projectId?: string;
  assigneeId?: string;
  status?: string;
  priority?: number;
  dueAt?: Date | null;
  createdAt?: Date | null;
}

export const TaskFields = ['id', 'title', 'projectId', 'assigneeId', 'status', 'priority', 'dueAt', 'createdAt'] as const;
export type TaskField = (typeof TaskFields)[number];

/** Construct a new Task with sensible defaults for optional fields. */
export function makeTask(input: Partial<Task> & { id: string }): Task {
  return {
    id: input.id,
    title: input.title ?? '',
    projectId: input.projectId ?? '',
    assigneeId: input.assigneeId ?? '',
    status: input.status ?? '',
    priority: input.priority ?? 0,
    dueAt: input.dueAt ?? null,
    createdAt: input.createdAt ?? null,
  } as Task;
}

/** Shallow merge for partial updates. Reject identifier mutation. */
export function updateTask(current: Task, patch: TaskUpdate): Task {
  const merged: Task = { ...current };
  for (const key of Object.keys(patch) as TaskField[]) {
    if (key === 'id') continue;
    const value = (patch as Record<string, unknown>)[key];
    if (value === undefined) continue;
    (merged as Record<string, unknown>)[key] = value;
  }
  return merged;
}

export function pickTaskFields(entity: Task, fields: TaskField[]): Partial<Task> {
  const out: Partial<Task> = {};
  for (const f of fields) {
    (out as Record<string, unknown>)[f] = (entity as Record<string, unknown>)[f];
  }
  return out;
}

export function cloneTask(entity: Task): Task {
  return JSON.parse(JSON.stringify(entity)) as Task;
}
