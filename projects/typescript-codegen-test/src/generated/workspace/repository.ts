// AUTOGENERATED from codegen/entities.yaml + codegen/templates/repository.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import type { Workspace, WorkspaceCreate, WorkspaceUpdate } from './model';
import { makeWorkspace, updateWorkspace } from './model';

/**
 * In-memory store. Methods are synchronous on purpose — this is a fixture, not a database.
 */
export class WorkspaceRepository {
  private readonly byId = new Map<string, Workspace>();
  private nextId = 1;

  all(): Workspace[] {
    return Array.from(this.byId.values());
  }

  count(): number {
    return this.byId.size;
  }

  findById(id: string): Workspace | undefined {
    return this.byId.get(id);
  }

  requireById(id: string): Workspace {
    const found = this.byId.get(id);
    if (!found) throw new WorkspaceNotFoundError(id);
    return found;
  }

  findFirst(predicate: (entity: Workspace) => boolean): Workspace | undefined {
    for (const entity of this.byId.values()) {
      if (predicate(entity)) return entity;
    }
    return undefined;
  }

  filter(predicate: (entity: Workspace) => boolean): Workspace[] {
    const out: Workspace[] = [];
    for (const entity of this.byId.values()) {
      if (predicate(entity)) out.push(entity);
    }
    return out;
  }

  paginate(offset: number, limit: number): Workspace[] {
    const out: Workspace[] = [];
    let i = 0;
    for (const entity of this.byId.values()) {
      if (i >= offset && i < offset + limit) out.push(entity);
      i++;
      if (i >= offset + limit) break;
    }
    return out;
  }

  insert(create: WorkspaceCreate): Workspace {
    const id = `${this.nextId++}`;
    const entity = makeWorkspace({ ...create, id } as Partial<Workspace> & { id: string });
    this.byId.set(id, entity);
    return entity;
  }

  update(id: string, patch: WorkspaceUpdate): Workspace {
    const current = this.requireById(id);
    const next = updateWorkspace(current, patch);
    this.byId.set(id, next);
    return next;
  }

  upsert(create: WorkspaceCreate, predicate: (existing: Workspace) => boolean): Workspace {
    const existing = this.findFirst(predicate);
    if (existing) return existing;
    return this.insert(create);
  }

  delete(id: string): boolean {
    return this.byId.delete(id);
  }

  clear(): void {
    this.byId.clear();
    this.nextId = 1;
  }

  sortedBy<K extends keyof Workspace>(key: K, order: 'asc' | 'desc' = 'asc'): Workspace[] {
    const items = this.all();
    const dir = order === 'asc' ? 1 : -1;
    items.sort((a, b) => {
      const av = a[key] as unknown;
      const bv = b[key] as unknown;
      if (av === bv) return 0;
      if (av === null || av === undefined) return -dir;
      if (bv === null || bv === undefined) return dir;
      return (av as number) < (bv as number) ? -dir : dir;
    });
    return items;
  }

  batchInsert(items: WorkspaceCreate[]): Workspace[] {
    return items.map((item) => this.insert(item));
  }

  batchUpdate(updates: Array<{ id: string; patch: WorkspaceUpdate }>): Workspace[] {
    const out: Workspace[] = [];
    for (const u of updates) {
      try { out.push(this.update(u.id, u.patch)); } catch { /* swallow missing */ }
    }
    return out;
  }

  replaceAll(items: Workspace[]): void {
    this.byId.clear();
    for (const item of items) this.byId.set(item.id, item);
  }
}

export class WorkspaceNotFoundError extends Error {
  constructor(public readonly id: string) {
    super(`Workspace not found: ${id}`);
    this.name = 'WorkspaceNotFoundError';
  }
}
