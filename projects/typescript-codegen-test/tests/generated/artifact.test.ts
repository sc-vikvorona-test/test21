// AUTOGENERATED from codegen/entities.yaml + codegen/templates/test.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import { describe, it, expect, beforeEach } from 'vitest';
import { ArtifactRepository, ArtifactNotFoundError } from '../../src/generated/artifact/repository';
import { ArtifactService, ArtifactValidationError } from '../../src/generated/artifact/service';
import { ArtifactController } from '../../src/generated/artifact/controller';

const minimalArtifactCreate = (): unknown => ({
  pipelineRunId: 'pipelineRunId-1',
  name: 'name-1',
  contentType: 'contentType-1',
  amountCents: 1,
  expiresAt: null,
});

describe('Artifact repository', () => {
  let repo: ArtifactRepository;
  beforeEach(() => { repo = new ArtifactRepository(); });

  it('inserts an entity and assigns an id', () => {
    const created = repo.insert(minimalArtifactCreate() as never);
    expect(created.id).toBeTruthy();
    expect(repo.findById(created.id)).toBeDefined();
  });

  it('updates an existing entity', () => {
    const created = repo.insert(minimalArtifactCreate() as never);
    const next = repo.update(created.id, {} as never);
    expect(next.id).toBe(created.id);
  });

  it('throws when updating a missing entity', () => {
    expect(() => repo.update('does-not-exist', {} as never)).toThrow(ArtifactNotFoundError);
  });

  it('paginates correctly', () => {
    for (let i = 0; i < 12; i++) repo.insert(minimalArtifactCreate() as never);
    const page = repo.paginate(5, 4);
    expect(page).toHaveLength(4);
  });

  it('deletes an entity', () => {
    const created = repo.insert(minimalArtifactCreate() as never);
    expect(repo.delete(created.id)).toBe(true);
    expect(repo.findById(created.id)).toBeUndefined();
  });

  it('batch inserts and updates', () => {
    const created = repo.batchInsert([
      minimalArtifactCreate() as never,
      minimalArtifactCreate() as never,
    ]);
    const updated = repo.batchUpdate([
      { id: created[0].id, patch: {} as never },
      { id: 'missing', patch: {} as never },
    ]);
    expect(updated).toHaveLength(1);
  });

  it('upserts using a predicate', () => {
    const first = repo.upsert(minimalArtifactCreate() as never, () => false);
    const second = repo.upsert(minimalArtifactCreate() as never, (e) => e.id === first.id);
    expect(second.id).toBe(first.id);
  });
});

describe('Artifact service', () => {
  let repo: ArtifactRepository;
  let service: ArtifactService;
  beforeEach(() => {
    repo = new ArtifactRepository();
    service = new ArtifactService({ repository: repo });
  });

  it('creates an entity', () => {
    const created = service.create(minimalArtifactCreate() as never);
    expect(service.exists(created.id)).toBe(true);
  });

  it('updates an entity', () => {
    const created = service.create(minimalArtifactCreate() as never);
    const updated = service.update(created.id, {} as never);
    expect(updated.id).toBe(created.id);
  });

  it('throws when getting a missing entity', () => {
    expect(() => service.get('missing')).toThrow(ArtifactNotFoundError);
  });

  it('lists with pagination', () => {
    for (let i = 0; i < 7; i++) service.create(minimalArtifactCreate() as never);
    const page = service.list({ offset: 0, limit: 5 });
    expect(page).toHaveLength(5);
  });
});

describe('Artifact controller', () => {
  let repo: ArtifactRepository;
  let service: ArtifactService;
  let ctrl: ArtifactController;
  beforeEach(() => {
    repo = new ArtifactRepository();
    service = new ArtifactService({ repository: repo });
    ctrl = new ArtifactController(service);
  });

  it('returns 404 on GET for missing id', () => {
    const res = ctrl.handleGet({ method: 'GET', path: '/artifacts/missing', params: { id: 'missing' }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(404);
  });

  it('returns 400 when offset is invalid', () => {
    const res = ctrl.handleList({ method: 'GET', path: '/artifacts', params: {}, query: { offset: '-1' }, body: null, headers: {} });
    expect(res.status).toBe(400);
  });

  it('returns 201 on POST create', () => {
    const res = ctrl.handleCreate({ method: 'POST', path: '/artifacts', params: {}, query: {}, body: minimalArtifactCreate(), headers: {} });
    expect(res.status).toBe(201);
  });

  it('returns 204 on DELETE existing', () => {
    const created = service.create(minimalArtifactCreate() as never);
    const res = ctrl.handleDelete({ method: 'DELETE', path: '/artifacts/' + created.id, params: { id: created.id }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(204);
  });
});
