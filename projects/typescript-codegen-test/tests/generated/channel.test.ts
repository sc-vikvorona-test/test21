// AUTOGENERATED from codegen/entities.yaml + codegen/templates/test.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import { describe, it, expect, beforeEach } from 'vitest';
import { ChannelRepository, ChannelNotFoundError } from '../../src/generated/channel/repository';
import { ChannelService, ChannelValidationError } from '../../src/generated/channel/service';
import { ChannelController } from '../../src/generated/channel/controller';

const minimalChannelCreate = (): unknown => ({
  name: 'name-1',
  workspaceId: 'workspaceId-1',
  visibility: 'visibility-1',
  memberCount: 1,
});

describe('Channel repository', () => {
  let repo: ChannelRepository;
  beforeEach(() => { repo = new ChannelRepository(); });

  it('inserts an entity and assigns an id', () => {
    const created = repo.insert(minimalChannelCreate() as never);
    expect(created.id).toBeTruthy();
    expect(repo.findById(created.id)).toBeDefined();
  });

  it('updates an existing entity', () => {
    const created = repo.insert(minimalChannelCreate() as never);
    const next = repo.update(created.id, {} as never);
    expect(next.id).toBe(created.id);
  });

  it('throws when updating a missing entity', () => {
    expect(() => repo.update('does-not-exist', {} as never)).toThrow(ChannelNotFoundError);
  });

  it('paginates correctly', () => {
    for (let i = 0; i < 12; i++) repo.insert(minimalChannelCreate() as never);
    const page = repo.paginate(5, 4);
    expect(page).toHaveLength(4);
  });

  it('deletes an entity', () => {
    const created = repo.insert(minimalChannelCreate() as never);
    expect(repo.delete(created.id)).toBe(true);
    expect(repo.findById(created.id)).toBeUndefined();
  });

  it('batch inserts and updates', () => {
    const created = repo.batchInsert([
      minimalChannelCreate() as never,
      minimalChannelCreate() as never,
    ]);
    const updated = repo.batchUpdate([
      { id: created[0].id, patch: {} as never },
      { id: 'missing', patch: {} as never },
    ]);
    expect(updated).toHaveLength(1);
  });

  it('upserts using a predicate', () => {
    const first = repo.upsert(minimalChannelCreate() as never, () => false);
    const second = repo.upsert(minimalChannelCreate() as never, (e) => e.id === first.id);
    expect(second.id).toBe(first.id);
  });
});

describe('Channel service', () => {
  let repo: ChannelRepository;
  let service: ChannelService;
  beforeEach(() => {
    repo = new ChannelRepository();
    service = new ChannelService({ repository: repo });
  });

  it('creates an entity', () => {
    const created = service.create(minimalChannelCreate() as never);
    expect(service.exists(created.id)).toBe(true);
  });

  it('updates an entity', () => {
    const created = service.create(minimalChannelCreate() as never);
    const updated = service.update(created.id, {} as never);
    expect(updated.id).toBe(created.id);
  });

  it('throws when getting a missing entity', () => {
    expect(() => service.get('missing')).toThrow(ChannelNotFoundError);
  });

  it('lists with pagination', () => {
    for (let i = 0; i < 7; i++) service.create(minimalChannelCreate() as never);
    const page = service.list({ offset: 0, limit: 5 });
    expect(page).toHaveLength(5);
  });
});

describe('Channel controller', () => {
  let repo: ChannelRepository;
  let service: ChannelService;
  let ctrl: ChannelController;
  beforeEach(() => {
    repo = new ChannelRepository();
    service = new ChannelService({ repository: repo });
    ctrl = new ChannelController(service);
  });

  it('returns 404 on GET for missing id', () => {
    const res = ctrl.handleGet({ method: 'GET', path: '/channels/missing', params: { id: 'missing' }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(404);
  });

  it('returns 400 when offset is invalid', () => {
    const res = ctrl.handleList({ method: 'GET', path: '/channels', params: {}, query: { offset: '-1' }, body: null, headers: {} });
    expect(res.status).toBe(400);
  });

  it('returns 201 on POST create', () => {
    const res = ctrl.handleCreate({ method: 'POST', path: '/channels', params: {}, query: {}, body: minimalChannelCreate(), headers: {} });
    expect(res.status).toBe(201);
  });

  it('returns 204 on DELETE existing', () => {
    const created = service.create(minimalChannelCreate() as never);
    const res = ctrl.handleDelete({ method: 'DELETE', path: '/channels/' + created.id, params: { id: created.id }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(204);
  });
});
