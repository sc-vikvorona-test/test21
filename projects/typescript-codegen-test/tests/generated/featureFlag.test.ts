// AUTOGENERATED from codegen/entities.yaml + codegen/templates/test.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import { describe, it, expect, beforeEach } from 'vitest';
import { FeatureFlagRepository, FeatureFlagNotFoundError } from '../../src/generated/featureFlag/repository';
import { FeatureFlagService, FeatureFlagValidationError } from '../../src/generated/featureFlag/service';
import { FeatureFlagController } from '../../src/generated/featureFlag/controller';

const minimalFeatureFlagCreate = (): unknown => ({
  key: 'key-1',
  description: 'description-1',
  rolloutPercent: 1,
  enabled: false,
});

describe('FeatureFlag repository', () => {
  let repo: FeatureFlagRepository;
  beforeEach(() => { repo = new FeatureFlagRepository(); });

  it('inserts an entity and assigns an id', () => {
    const created = repo.insert(minimalFeatureFlagCreate() as never);
    expect(created.id).toBeTruthy();
    expect(repo.findById(created.id)).toBeDefined();
  });

  it('updates an existing entity', () => {
    const created = repo.insert(minimalFeatureFlagCreate() as never);
    const next = repo.update(created.id, {} as never);
    expect(next.id).toBe(created.id);
  });

  it('throws when updating a missing entity', () => {
    expect(() => repo.update('does-not-exist', {} as never)).toThrow(FeatureFlagNotFoundError);
  });

  it('paginates correctly', () => {
    for (let i = 0; i < 12; i++) repo.insert(minimalFeatureFlagCreate() as never);
    const page = repo.paginate(5, 4);
    expect(page).toHaveLength(4);
  });

  it('deletes an entity', () => {
    const created = repo.insert(minimalFeatureFlagCreate() as never);
    expect(repo.delete(created.id)).toBe(true);
    expect(repo.findById(created.id)).toBeUndefined();
  });

  it('batch inserts and updates', () => {
    const created = repo.batchInsert([
      minimalFeatureFlagCreate() as never,
      minimalFeatureFlagCreate() as never,
    ]);
    const updated = repo.batchUpdate([
      { id: created[0].id, patch: {} as never },
      { id: 'missing', patch: {} as never },
    ]);
    expect(updated).toHaveLength(1);
  });

  it('upserts using a predicate', () => {
    const first = repo.upsert(minimalFeatureFlagCreate() as never, () => false);
    const second = repo.upsert(minimalFeatureFlagCreate() as never, (e) => e.id === first.id);
    expect(second.id).toBe(first.id);
  });
});

describe('FeatureFlag service', () => {
  let repo: FeatureFlagRepository;
  let service: FeatureFlagService;
  beforeEach(() => {
    repo = new FeatureFlagRepository();
    service = new FeatureFlagService({ repository: repo });
  });

  it('creates an entity', () => {
    const created = service.create(minimalFeatureFlagCreate() as never);
    expect(service.exists(created.id)).toBe(true);
  });

  it('updates an entity', () => {
    const created = service.create(minimalFeatureFlagCreate() as never);
    const updated = service.update(created.id, {} as never);
    expect(updated.id).toBe(created.id);
  });

  it('throws when getting a missing entity', () => {
    expect(() => service.get('missing')).toThrow(FeatureFlagNotFoundError);
  });

  it('lists with pagination', () => {
    for (let i = 0; i < 7; i++) service.create(minimalFeatureFlagCreate() as never);
    const page = service.list({ offset: 0, limit: 5 });
    expect(page).toHaveLength(5);
  });
});

describe('FeatureFlag controller', () => {
  let repo: FeatureFlagRepository;
  let service: FeatureFlagService;
  let ctrl: FeatureFlagController;
  beforeEach(() => {
    repo = new FeatureFlagRepository();
    service = new FeatureFlagService({ repository: repo });
    ctrl = new FeatureFlagController(service);
  });

  it('returns 404 on GET for missing id', () => {
    const res = ctrl.handleGet({ method: 'GET', path: '/featureFlags/missing', params: { id: 'missing' }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(404);
  });

  it('returns 400 when offset is invalid', () => {
    const res = ctrl.handleList({ method: 'GET', path: '/featureFlags', params: {}, query: { offset: '-1' }, body: null, headers: {} });
    expect(res.status).toBe(400);
  });

  it('returns 201 on POST create', () => {
    const res = ctrl.handleCreate({ method: 'POST', path: '/featureFlags', params: {}, query: {}, body: minimalFeatureFlagCreate(), headers: {} });
    expect(res.status).toBe(201);
  });

  it('returns 204 on DELETE existing', () => {
    const created = service.create(minimalFeatureFlagCreate() as never);
    const res = ctrl.handleDelete({ method: 'DELETE', path: '/featureFlags/' + created.id, params: { id: created.id }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(204);
  });
});
