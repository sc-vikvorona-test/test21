// AUTOGENERATED from codegen/entities.yaml + codegen/templates/test.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import { describe, it, expect, beforeEach } from 'vitest';
import { InvitationRepository, InvitationNotFoundError } from '../../src/generated/invitation/repository';
import { InvitationService, InvitationValidationError } from '../../src/generated/invitation/service';
import { InvitationController } from '../../src/generated/invitation/controller';

const minimalInvitationCreate = (): unknown => ({
  email: 'email-1',
  organizationId: 'organizationId-1',
  inviterId: 'inviterId-1',
  token: 'token-1',
  acceptedAt: null,
  expiresAt: null,
});

describe('Invitation repository', () => {
  let repo: InvitationRepository;
  beforeEach(() => { repo = new InvitationRepository(); });

  it('inserts an entity and assigns an id', () => {
    const created = repo.insert(minimalInvitationCreate() as never);
    expect(created.id).toBeTruthy();
    expect(repo.findById(created.id)).toBeDefined();
  });

  it('updates an existing entity', () => {
    const created = repo.insert(minimalInvitationCreate() as never);
    const next = repo.update(created.id, {} as never);
    expect(next.id).toBe(created.id);
  });

  it('throws when updating a missing entity', () => {
    expect(() => repo.update('does-not-exist', {} as never)).toThrow(InvitationNotFoundError);
  });

  it('paginates correctly', () => {
    for (let i = 0; i < 12; i++) repo.insert(minimalInvitationCreate() as never);
    const page = repo.paginate(5, 4);
    expect(page).toHaveLength(4);
  });

  it('deletes an entity', () => {
    const created = repo.insert(minimalInvitationCreate() as never);
    expect(repo.delete(created.id)).toBe(true);
    expect(repo.findById(created.id)).toBeUndefined();
  });

  it('batch inserts and updates', () => {
    const created = repo.batchInsert([
      minimalInvitationCreate() as never,
      minimalInvitationCreate() as never,
    ]);
    const updated = repo.batchUpdate([
      { id: created[0].id, patch: {} as never },
      { id: 'missing', patch: {} as never },
    ]);
    expect(updated).toHaveLength(1);
  });

  it('upserts using a predicate', () => {
    const first = repo.upsert(minimalInvitationCreate() as never, () => false);
    const second = repo.upsert(minimalInvitationCreate() as never, (e) => e.id === first.id);
    expect(second.id).toBe(first.id);
  });
});

describe('Invitation service', () => {
  let repo: InvitationRepository;
  let service: InvitationService;
  beforeEach(() => {
    repo = new InvitationRepository();
    service = new InvitationService({ repository: repo });
  });

  it('creates an entity', () => {
    const created = service.create(minimalInvitationCreate() as never);
    expect(service.exists(created.id)).toBe(true);
  });

  it('updates an entity', () => {
    const created = service.create(minimalInvitationCreate() as never);
    const updated = service.update(created.id, {} as never);
    expect(updated.id).toBe(created.id);
  });

  it('throws when getting a missing entity', () => {
    expect(() => service.get('missing')).toThrow(InvitationNotFoundError);
  });

  it('lists with pagination', () => {
    for (let i = 0; i < 7; i++) service.create(minimalInvitationCreate() as never);
    const page = service.list({ offset: 0, limit: 5 });
    expect(page).toHaveLength(5);
  });
});

describe('Invitation controller', () => {
  let repo: InvitationRepository;
  let service: InvitationService;
  let ctrl: InvitationController;
  beforeEach(() => {
    repo = new InvitationRepository();
    service = new InvitationService({ repository: repo });
    ctrl = new InvitationController(service);
  });

  it('returns 404 on GET for missing id', () => {
    const res = ctrl.handleGet({ method: 'GET', path: '/invitations/missing', params: { id: 'missing' }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(404);
  });

  it('returns 400 when offset is invalid', () => {
    const res = ctrl.handleList({ method: 'GET', path: '/invitations', params: {}, query: { offset: '-1' }, body: null, headers: {} });
    expect(res.status).toBe(400);
  });

  it('returns 201 on POST create', () => {
    const res = ctrl.handleCreate({ method: 'POST', path: '/invitations', params: {}, query: {}, body: minimalInvitationCreate(), headers: {} });
    expect(res.status).toBe(201);
  });

  it('returns 204 on DELETE existing', () => {
    const created = service.create(minimalInvitationCreate() as never);
    const res = ctrl.handleDelete({ method: 'DELETE', path: '/invitations/' + created.id, params: { id: created.id }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(204);
  });
});
