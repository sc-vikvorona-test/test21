// AUTOGENERATED from codegen/entities.yaml + codegen/templates/test.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import { describe, it, expect, beforeEach } from 'vitest';
import { OauthGrantRepository, OauthGrantNotFoundError } from '../../src/generated/oauthGrant/repository';
import { OauthGrantService, OauthGrantValidationError } from '../../src/generated/oauthGrant/service';
import { OauthGrantController } from '../../src/generated/oauthGrant/controller';

const minimalOauthGrantCreate = (): unknown => ({
  userId: 'userId-1',
  clientId: 'clientId-1',
  scopes: [],
  expiresAt: null,
  revokedAt: null,
});

describe('OauthGrant repository', () => {
  let repo: OauthGrantRepository;
  beforeEach(() => { repo = new OauthGrantRepository(); });

  it('inserts an entity and assigns an id', () => {
    const created = repo.insert(minimalOauthGrantCreate() as never);
    expect(created.id).toBeTruthy();
    expect(repo.findById(created.id)).toBeDefined();
  });

  it('updates an existing entity', () => {
    const created = repo.insert(minimalOauthGrantCreate() as never);
    const next = repo.update(created.id, {} as never);
    expect(next.id).toBe(created.id);
  });

  it('throws when updating a missing entity', () => {
    expect(() => repo.update('does-not-exist', {} as never)).toThrow(OauthGrantNotFoundError);
  });

  it('paginates correctly', () => {
    for (let i = 0; i < 12; i++) repo.insert(minimalOauthGrantCreate() as never);
    const page = repo.paginate(5, 4);
    expect(page).toHaveLength(4);
  });

  it('deletes an entity', () => {
    const created = repo.insert(minimalOauthGrantCreate() as never);
    expect(repo.delete(created.id)).toBe(true);
    expect(repo.findById(created.id)).toBeUndefined();
  });

  it('batch inserts and updates', () => {
    const created = repo.batchInsert([
      minimalOauthGrantCreate() as never,
      minimalOauthGrantCreate() as never,
    ]);
    const updated = repo.batchUpdate([
      { id: created[0].id, patch: {} as never },
      { id: 'missing', patch: {} as never },
    ]);
    expect(updated).toHaveLength(1);
  });

  it('upserts using a predicate', () => {
    const first = repo.upsert(minimalOauthGrantCreate() as never, () => false);
    const second = repo.upsert(minimalOauthGrantCreate() as never, (e) => e.id === first.id);
    expect(second.id).toBe(first.id);
  });
});

describe('OauthGrant service', () => {
  let repo: OauthGrantRepository;
  let service: OauthGrantService;
  beforeEach(() => {
    repo = new OauthGrantRepository();
    service = new OauthGrantService({ repository: repo });
  });

  it('creates an entity', () => {
    const created = service.create(minimalOauthGrantCreate() as never);
    expect(service.exists(created.id)).toBe(true);
  });

  it('updates an entity', () => {
    const created = service.create(minimalOauthGrantCreate() as never);
    const updated = service.update(created.id, {} as never);
    expect(updated.id).toBe(created.id);
  });

  it('throws when getting a missing entity', () => {
    expect(() => service.get('missing')).toThrow(OauthGrantNotFoundError);
  });

  it('lists with pagination', () => {
    for (let i = 0; i < 7; i++) service.create(minimalOauthGrantCreate() as never);
    const page = service.list({ offset: 0, limit: 5 });
    expect(page).toHaveLength(5);
  });
});

describe('OauthGrant controller', () => {
  let repo: OauthGrantRepository;
  let service: OauthGrantService;
  let ctrl: OauthGrantController;
  beforeEach(() => {
    repo = new OauthGrantRepository();
    service = new OauthGrantService({ repository: repo });
    ctrl = new OauthGrantController(service);
  });

  it('returns 404 on GET for missing id', () => {
    const res = ctrl.handleGet({ method: 'GET', path: '/oauthGrants/missing', params: { id: 'missing' }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(404);
  });

  it('returns 400 when offset is invalid', () => {
    const res = ctrl.handleList({ method: 'GET', path: '/oauthGrants', params: {}, query: { offset: '-1' }, body: null, headers: {} });
    expect(res.status).toBe(400);
  });

  it('returns 201 on POST create', () => {
    const res = ctrl.handleCreate({ method: 'POST', path: '/oauthGrants', params: {}, query: {}, body: minimalOauthGrantCreate(), headers: {} });
    expect(res.status).toBe(201);
  });

  it('returns 204 on DELETE existing', () => {
    const created = service.create(minimalOauthGrantCreate() as never);
    const res = ctrl.handleDelete({ method: 'DELETE', path: '/oauthGrants/' + created.id, params: { id: created.id }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(204);
  });
});
