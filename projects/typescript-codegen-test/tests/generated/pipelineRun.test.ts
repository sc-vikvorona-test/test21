// AUTOGENERATED from codegen/entities.yaml + codegen/templates/test.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import { describe, it, expect, beforeEach } from 'vitest';
import { PipelineRunRepository, PipelineRunNotFoundError } from '../../src/generated/pipelineRun/repository';
import { PipelineRunService, PipelineRunValidationError } from '../../src/generated/pipelineRun/service';
import { PipelineRunController } from '../../src/generated/pipelineRun/controller';

const minimalPipelineRunCreate = (): unknown => ({
  pipelineId: 'pipelineId-1',
  status: 'status-1',
  triggeredBy: 'triggeredBy-1',
  startedAt: null,
  completedAt: null,
});

describe('PipelineRun repository', () => {
  let repo: PipelineRunRepository;
  beforeEach(() => { repo = new PipelineRunRepository(); });

  it('inserts an entity and assigns an id', () => {
    const created = repo.insert(minimalPipelineRunCreate() as never);
    expect(created.id).toBeTruthy();
    expect(repo.findById(created.id)).toBeDefined();
  });

  it('updates an existing entity', () => {
    const created = repo.insert(minimalPipelineRunCreate() as never);
    const next = repo.update(created.id, {} as never);
    expect(next.id).toBe(created.id);
  });

  it('throws when updating a missing entity', () => {
    expect(() => repo.update('does-not-exist', {} as never)).toThrow(PipelineRunNotFoundError);
  });

  it('paginates correctly', () => {
    for (let i = 0; i < 12; i++) repo.insert(minimalPipelineRunCreate() as never);
    const page = repo.paginate(5, 4);
    expect(page).toHaveLength(4);
  });

  it('deletes an entity', () => {
    const created = repo.insert(minimalPipelineRunCreate() as never);
    expect(repo.delete(created.id)).toBe(true);
    expect(repo.findById(created.id)).toBeUndefined();
  });

  it('batch inserts and updates', () => {
    const created = repo.batchInsert([
      minimalPipelineRunCreate() as never,
      minimalPipelineRunCreate() as never,
    ]);
    const updated = repo.batchUpdate([
      { id: created[0].id, patch: {} as never },
      { id: 'missing', patch: {} as never },
    ]);
    expect(updated).toHaveLength(1);
  });

  it('upserts using a predicate', () => {
    const first = repo.upsert(minimalPipelineRunCreate() as never, () => false);
    const second = repo.upsert(minimalPipelineRunCreate() as never, (e) => e.id === first.id);
    expect(second.id).toBe(first.id);
  });
});

describe('PipelineRun service', () => {
  let repo: PipelineRunRepository;
  let service: PipelineRunService;
  beforeEach(() => {
    repo = new PipelineRunRepository();
    service = new PipelineRunService({ repository: repo });
  });

  it('creates an entity', () => {
    const created = service.create(minimalPipelineRunCreate() as never);
    expect(service.exists(created.id)).toBe(true);
  });

  it('updates an entity', () => {
    const created = service.create(minimalPipelineRunCreate() as never);
    const updated = service.update(created.id, {} as never);
    expect(updated.id).toBe(created.id);
  });

  it('throws when getting a missing entity', () => {
    expect(() => service.get('missing')).toThrow(PipelineRunNotFoundError);
  });

  it('lists with pagination', () => {
    for (let i = 0; i < 7; i++) service.create(minimalPipelineRunCreate() as never);
    const page = service.list({ offset: 0, limit: 5 });
    expect(page).toHaveLength(5);
  });
});

describe('PipelineRun controller', () => {
  let repo: PipelineRunRepository;
  let service: PipelineRunService;
  let ctrl: PipelineRunController;
  beforeEach(() => {
    repo = new PipelineRunRepository();
    service = new PipelineRunService({ repository: repo });
    ctrl = new PipelineRunController(service);
  });

  it('returns 404 on GET for missing id', () => {
    const res = ctrl.handleGet({ method: 'GET', path: '/pipelineRuns/missing', params: { id: 'missing' }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(404);
  });

  it('returns 400 when offset is invalid', () => {
    const res = ctrl.handleList({ method: 'GET', path: '/pipelineRuns', params: {}, query: { offset: '-1' }, body: null, headers: {} });
    expect(res.status).toBe(400);
  });

  it('returns 201 on POST create', () => {
    const res = ctrl.handleCreate({ method: 'POST', path: '/pipelineRuns', params: {}, query: {}, body: minimalPipelineRunCreate(), headers: {} });
    expect(res.status).toBe(201);
  });

  it('returns 204 on DELETE existing', () => {
    const created = service.create(minimalPipelineRunCreate() as never);
    const res = ctrl.handleDelete({ method: 'DELETE', path: '/pipelineRuns/' + created.id, params: { id: created.id }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(204);
  });
});
