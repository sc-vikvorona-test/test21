// AUTOGENERATED from codegen/entities.yaml + codegen/templates/test.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import { describe, it, expect, beforeEach } from 'vitest';
import { ReviewRepository, ReviewNotFoundError } from '../../src/generated/review/repository';
import { ReviewService, ReviewValidationError } from '../../src/generated/review/service';
import { ReviewController } from '../../src/generated/review/controller';

const minimalReviewCreate = (): unknown => ({
  pullRequestId: 'pullRequestId-1',
  reviewerId: 'reviewerId-1',
  state: 'state-1',
  submittedAt: null,
  body: 'body-1',
});

describe('Review repository', () => {
  let repo: ReviewRepository;
  beforeEach(() => { repo = new ReviewRepository(); });

  it('inserts an entity and assigns an id', () => {
    const created = repo.insert(minimalReviewCreate() as never);
    expect(created.id).toBeTruthy();
    expect(repo.findById(created.id)).toBeDefined();
  });

  it('updates an existing entity', () => {
    const created = repo.insert(minimalReviewCreate() as never);
    const next = repo.update(created.id, {} as never);
    expect(next.id).toBe(created.id);
  });

  it('throws when updating a missing entity', () => {
    expect(() => repo.update('does-not-exist', {} as never)).toThrow(ReviewNotFoundError);
  });

  it('paginates correctly', () => {
    for (let i = 0; i < 12; i++) repo.insert(minimalReviewCreate() as never);
    const page = repo.paginate(5, 4);
    expect(page).toHaveLength(4);
  });

  it('deletes an entity', () => {
    const created = repo.insert(minimalReviewCreate() as never);
    expect(repo.delete(created.id)).toBe(true);
    expect(repo.findById(created.id)).toBeUndefined();
  });

  it('batch inserts and updates', () => {
    const created = repo.batchInsert([
      minimalReviewCreate() as never,
      minimalReviewCreate() as never,
    ]);
    const updated = repo.batchUpdate([
      { id: created[0].id, patch: {} as never },
      { id: 'missing', patch: {} as never },
    ]);
    expect(updated).toHaveLength(1);
  });

  it('upserts using a predicate', () => {
    const first = repo.upsert(minimalReviewCreate() as never, () => false);
    const second = repo.upsert(minimalReviewCreate() as never, (e) => e.id === first.id);
    expect(second.id).toBe(first.id);
  });
});

describe('Review service', () => {
  let repo: ReviewRepository;
  let service: ReviewService;
  beforeEach(() => {
    repo = new ReviewRepository();
    service = new ReviewService({ repository: repo });
  });

  it('creates an entity', () => {
    const created = service.create(minimalReviewCreate() as never);
    expect(service.exists(created.id)).toBe(true);
  });

  it('updates an entity', () => {
    const created = service.create(minimalReviewCreate() as never);
    const updated = service.update(created.id, {} as never);
    expect(updated.id).toBe(created.id);
  });

  it('throws when getting a missing entity', () => {
    expect(() => service.get('missing')).toThrow(ReviewNotFoundError);
  });

  it('lists with pagination', () => {
    for (let i = 0; i < 7; i++) service.create(minimalReviewCreate() as never);
    const page = service.list({ offset: 0, limit: 5 });
    expect(page).toHaveLength(5);
  });
});

describe('Review controller', () => {
  let repo: ReviewRepository;
  let service: ReviewService;
  let ctrl: ReviewController;
  beforeEach(() => {
    repo = new ReviewRepository();
    service = new ReviewService({ repository: repo });
    ctrl = new ReviewController(service);
  });

  it('returns 404 on GET for missing id', () => {
    const res = ctrl.handleGet({ method: 'GET', path: '/reviews/missing', params: { id: 'missing' }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(404);
  });

  it('returns 400 when offset is invalid', () => {
    const res = ctrl.handleList({ method: 'GET', path: '/reviews', params: {}, query: { offset: '-1' }, body: null, headers: {} });
    expect(res.status).toBe(400);
  });

  it('returns 201 on POST create', () => {
    const res = ctrl.handleCreate({ method: 'POST', path: '/reviews', params: {}, query: {}, body: minimalReviewCreate(), headers: {} });
    expect(res.status).toBe(201);
  });

  it('returns 204 on DELETE existing', () => {
    const created = service.create(minimalReviewCreate() as never);
    const res = ctrl.handleDelete({ method: 'DELETE', path: '/reviews/' + created.id, params: { id: created.id }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(204);
  });
});
