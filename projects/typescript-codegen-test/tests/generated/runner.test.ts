// AUTOGENERATED from codegen/entities.yaml + codegen/templates/test.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import { describe, it, expect, beforeEach } from 'vitest';
import { RunnerRepository, RunnerNotFoundError } from '../../src/generated/runner/repository';
import { RunnerService, RunnerValidationError } from '../../src/generated/runner/service';
import { RunnerController } from '../../src/generated/runner/controller';

const minimalRunnerCreate = (): unknown => ({
  name: 'name-1',
  kind: 'kind-1',
  ownerId: 'ownerId-1',
  active: false,
  lastSeenAt: null,
});

describe('Runner repository', () => {
  let repo: RunnerRepository;
  beforeEach(() => { repo = new RunnerRepository(); });

  it('inserts an entity and assigns an id', () => {
    const created = repo.insert(minimalRunnerCreate() as never);
    expect(created.id).toBeTruthy();
    expect(repo.findById(created.id)).toBeDefined();
  });

  it('updates an existing entity', () => {
    const created = repo.insert(minimalRunnerCreate() as never);
    const next = repo.update(created.id, {} as never);
    expect(next.id).toBe(created.id);
  });

  it('throws when updating a missing entity', () => {
    expect(() => repo.update('does-not-exist', {} as never)).toThrow(RunnerNotFoundError);
  });

  it('paginates correctly', () => {
    for (let i = 0; i < 12; i++) repo.insert(minimalRunnerCreate() as never);
    const page = repo.paginate(5, 4);
    expect(page).toHaveLength(4);
  });

  it('deletes an entity', () => {
    const created = repo.insert(minimalRunnerCreate() as never);
    expect(repo.delete(created.id)).toBe(true);
    expect(repo.findById(created.id)).toBeUndefined();
  });

  it('batch inserts and updates', () => {
    const created = repo.batchInsert([
      minimalRunnerCreate() as never,
      minimalRunnerCreate() as never,
    ]);
    const updated = repo.batchUpdate([
      { id: created[0].id, patch: {} as never },
      { id: 'missing', patch: {} as never },
    ]);
    expect(updated).toHaveLength(1);
  });

  it('upserts using a predicate', () => {
    const first = repo.upsert(minimalRunnerCreate() as never, () => false);
    const second = repo.upsert(minimalRunnerCreate() as never, (e) => e.id === first.id);
    expect(second.id).toBe(first.id);
  });
});

describe('Runner service', () => {
  let repo: RunnerRepository;
  let service: RunnerService;
  beforeEach(() => {
    repo = new RunnerRepository();
    service = new RunnerService({ repository: repo });
  });

  it('creates an entity', () => {
    const created = service.create(minimalRunnerCreate() as never);
    expect(service.exists(created.id)).toBe(true);
  });

  it('updates an entity', () => {
    const created = service.create(minimalRunnerCreate() as never);
    const updated = service.update(created.id, {} as never);
    expect(updated.id).toBe(created.id);
  });

  it('throws when getting a missing entity', () => {
    expect(() => service.get('missing')).toThrow(RunnerNotFoundError);
  });

  it('lists with pagination', () => {
    for (let i = 0; i < 7; i++) service.create(minimalRunnerCreate() as never);
    const page = service.list({ offset: 0, limit: 5 });
    expect(page).toHaveLength(5);
  });
});

describe('Runner controller', () => {
  let repo: RunnerRepository;
  let service: RunnerService;
  let ctrl: RunnerController;
  beforeEach(() => {
    repo = new RunnerRepository();
    service = new RunnerService({ repository: repo });
    ctrl = new RunnerController(service);
  });

  it('returns 404 on GET for missing id', () => {
    const res = ctrl.handleGet({ method: 'GET', path: '/runners/missing', params: { id: 'missing' }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(404);
  });

  it('returns 400 when offset is invalid', () => {
    const res = ctrl.handleList({ method: 'GET', path: '/runners', params: {}, query: { offset: '-1' }, body: null, headers: {} });
    expect(res.status).toBe(400);
  });

  it('returns 201 on POST create', () => {
    const res = ctrl.handleCreate({ method: 'POST', path: '/runners', params: {}, query: {}, body: minimalRunnerCreate(), headers: {} });
    expect(res.status).toBe(201);
  });

  it('returns 204 on DELETE existing', () => {
    const created = service.create(minimalRunnerCreate() as never);
    const res = ctrl.handleDelete({ method: 'DELETE', path: '/runners/' + created.id, params: { id: created.id }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(204);
  });
});
