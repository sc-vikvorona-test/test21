// AUTOGENERATED from codegen/entities.yaml + codegen/templates/test.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import { describe, it, expect, beforeEach } from 'vitest';
import { TeamRepository, TeamNotFoundError } from '../../src/generated/team/repository';
import { TeamService, TeamValidationError } from '../../src/generated/team/service';
import { TeamController } from '../../src/generated/team/controller';

const minimalTeamCreate = (): unknown => ({
  name: 'name-1',
  organizationId: 'organizationId-1',
  slug: 'slug-1',
  parentTeamId: 'parentTeamId-1',
  memberCount: 1,
});

describe('Team repository', () => {
  let repo: TeamRepository;
  beforeEach(() => { repo = new TeamRepository(); });

  it('inserts an entity and assigns an id', () => {
    const created = repo.insert(minimalTeamCreate() as never);
    expect(created.id).toBeTruthy();
    expect(repo.findById(created.id)).toBeDefined();
  });

  it('updates an existing entity', () => {
    const created = repo.insert(minimalTeamCreate() as never);
    const next = repo.update(created.id, {} as never);
    expect(next.id).toBe(created.id);
  });

  it('throws when updating a missing entity', () => {
    expect(() => repo.update('does-not-exist', {} as never)).toThrow(TeamNotFoundError);
  });

  it('paginates correctly', () => {
    for (let i = 0; i < 12; i++) repo.insert(minimalTeamCreate() as never);
    const page = repo.paginate(5, 4);
    expect(page).toHaveLength(4);
  });

  it('deletes an entity', () => {
    const created = repo.insert(minimalTeamCreate() as never);
    expect(repo.delete(created.id)).toBe(true);
    expect(repo.findById(created.id)).toBeUndefined();
  });

  it('batch inserts and updates', () => {
    const created = repo.batchInsert([
      minimalTeamCreate() as never,
      minimalTeamCreate() as never,
    ]);
    const updated = repo.batchUpdate([
      { id: created[0].id, patch: {} as never },
      { id: 'missing', patch: {} as never },
    ]);
    expect(updated).toHaveLength(1);
  });

  it('upserts using a predicate', () => {
    const first = repo.upsert(minimalTeamCreate() as never, () => false);
    const second = repo.upsert(minimalTeamCreate() as never, (e) => e.id === first.id);
    expect(second.id).toBe(first.id);
  });
});

describe('Team service', () => {
  let repo: TeamRepository;
  let service: TeamService;
  beforeEach(() => {
    repo = new TeamRepository();
    service = new TeamService({ repository: repo });
  });

  it('creates an entity', () => {
    const created = service.create(minimalTeamCreate() as never);
    expect(service.exists(created.id)).toBe(true);
  });

  it('updates an entity', () => {
    const created = service.create(minimalTeamCreate() as never);
    const updated = service.update(created.id, {} as never);
    expect(updated.id).toBe(created.id);
  });

  it('throws when getting a missing entity', () => {
    expect(() => service.get('missing')).toThrow(TeamNotFoundError);
  });

  it('lists with pagination', () => {
    for (let i = 0; i < 7; i++) service.create(minimalTeamCreate() as never);
    const page = service.list({ offset: 0, limit: 5 });
    expect(page).toHaveLength(5);
  });
});

describe('Team controller', () => {
  let repo: TeamRepository;
  let service: TeamService;
  let ctrl: TeamController;
  beforeEach(() => {
    repo = new TeamRepository();
    service = new TeamService({ repository: repo });
    ctrl = new TeamController(service);
  });

  it('returns 404 on GET for missing id', () => {
    const res = ctrl.handleGet({ method: 'GET', path: '/teams/missing', params: { id: 'missing' }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(404);
  });

  it('returns 400 when offset is invalid', () => {
    const res = ctrl.handleList({ method: 'GET', path: '/teams', params: {}, query: { offset: '-1' }, body: null, headers: {} });
    expect(res.status).toBe(400);
  });

  it('returns 201 on POST create', () => {
    const res = ctrl.handleCreate({ method: 'POST', path: '/teams', params: {}, query: {}, body: minimalTeamCreate(), headers: {} });
    expect(res.status).toBe(201);
  });

  it('returns 204 on DELETE existing', () => {
    const created = service.create(minimalTeamCreate() as never);
    const res = ctrl.handleDelete({ method: 'DELETE', path: '/teams/' + created.id, params: { id: created.id }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(204);
  });
});
