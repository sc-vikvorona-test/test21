// AUTOGENERATED from codegen/entities.yaml + codegen/templates/test.tmpl.
// Regenerate via: python codegen/generate.py
// Do not edit this file directly — changes will be lost on the next run.

import { describe, it, expect, beforeEach } from 'vitest';
import { ThreadRepository, ThreadNotFoundError } from '../../src/generated/thread/repository';
import { ThreadService, ThreadValidationError } from '../../src/generated/thread/service';
import { ThreadController } from '../../src/generated/thread/controller';

const minimalThreadCreate = (): unknown => ({
  channelId: 'channelId-1',
  parentMessageId: 'parentMessageId-1',
  memberCount: 1,
});

describe('Thread repository', () => {
  let repo: ThreadRepository;
  beforeEach(() => { repo = new ThreadRepository(); });

  it('inserts an entity and assigns an id', () => {
    const created = repo.insert(minimalThreadCreate() as never);
    expect(created.id).toBeTruthy();
    expect(repo.findById(created.id)).toBeDefined();
  });

  it('updates an existing entity', () => {
    const created = repo.insert(minimalThreadCreate() as never);
    const next = repo.update(created.id, {} as never);
    expect(next.id).toBe(created.id);
  });

  it('throws when updating a missing entity', () => {
    expect(() => repo.update('does-not-exist', {} as never)).toThrow(ThreadNotFoundError);
  });

  it('paginates correctly', () => {
    for (let i = 0; i < 12; i++) repo.insert(minimalThreadCreate() as never);
    const page = repo.paginate(5, 4);
    expect(page).toHaveLength(4);
  });

  it('deletes an entity', () => {
    const created = repo.insert(minimalThreadCreate() as never);
    expect(repo.delete(created.id)).toBe(true);
    expect(repo.findById(created.id)).toBeUndefined();
  });

  it('batch inserts and updates', () => {
    const created = repo.batchInsert([
      minimalThreadCreate() as never,
      minimalThreadCreate() as never,
    ]);
    const updated = repo.batchUpdate([
      { id: created[0].id, patch: {} as never },
      { id: 'missing', patch: {} as never },
    ]);
    expect(updated).toHaveLength(1);
  });

  it('upserts using a predicate', () => {
    const first = repo.upsert(minimalThreadCreate() as never, () => false);
    const second = repo.upsert(minimalThreadCreate() as never, (e) => e.id === first.id);
    expect(second.id).toBe(first.id);
  });
});

describe('Thread service', () => {
  let repo: ThreadRepository;
  let service: ThreadService;
  beforeEach(() => {
    repo = new ThreadRepository();
    service = new ThreadService({ repository: repo });
  });

  it('creates an entity', () => {
    const created = service.create(minimalThreadCreate() as never);
    expect(service.exists(created.id)).toBe(true);
  });

  it('updates an entity', () => {
    const created = service.create(minimalThreadCreate() as never);
    const updated = service.update(created.id, {} as never);
    expect(updated.id).toBe(created.id);
  });

  it('throws when getting a missing entity', () => {
    expect(() => service.get('missing')).toThrow(ThreadNotFoundError);
  });

  it('lists with pagination', () => {
    for (let i = 0; i < 7; i++) service.create(minimalThreadCreate() as never);
    const page = service.list({ offset: 0, limit: 5 });
    expect(page).toHaveLength(5);
  });
});

describe('Thread controller', () => {
  let repo: ThreadRepository;
  let service: ThreadService;
  let ctrl: ThreadController;
  beforeEach(() => {
    repo = new ThreadRepository();
    service = new ThreadService({ repository: repo });
    ctrl = new ThreadController(service);
  });

  it('returns 404 on GET for missing id', () => {
    const res = ctrl.handleGet({ method: 'GET', path: '/threads/missing', params: { id: 'missing' }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(404);
  });

  it('returns 400 when offset is invalid', () => {
    const res = ctrl.handleList({ method: 'GET', path: '/threads', params: {}, query: { offset: '-1' }, body: null, headers: {} });
    expect(res.status).toBe(400);
  });

  it('returns 201 on POST create', () => {
    const res = ctrl.handleCreate({ method: 'POST', path: '/threads', params: {}, query: {}, body: minimalThreadCreate(), headers: {} });
    expect(res.status).toBe(201);
  });

  it('returns 204 on DELETE existing', () => {
    const created = service.create(minimalThreadCreate() as never);
    const res = ctrl.handleDelete({ method: 'DELETE', path: '/threads/' + created.id, params: { id: created.id }, query: {}, body: null, headers: {} });
    expect(res.status).toBe(204);
  });
});
